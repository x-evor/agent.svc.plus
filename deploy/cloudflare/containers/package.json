{
  "name": "agent-svc-plus-cf-containers",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.25"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
